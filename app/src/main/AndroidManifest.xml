<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tutor.ajakteman">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".tentor.WelcomeScreenTentor" />
        <activity android:name=".siswa.WelcomeScreenSiswa" />
        <activity android:name=".RegisterSiswaActivity" />
        <activity android:name=".WelcomeScreenActivity" />
        <activity android:name=".RegisterTentorActivity"></activity>
        <activity android:name=".AfterTentorRegistrationActivity" />
        <activity android:name=".pembayaran.PembayaranActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".siswa.kelas.lobbykelas.LobbyKelasActivity"></activity>
    </application>

</manifest>